#ifndef QUICKSORT_HPP
#define QUICKSORT_HPP

// The first parameter is the array that needs to be sorted, and the second
// one is a function that compares elements (if second is lower than the second)
template <typename T>
void Quicksort(T *array, bool (*compare)(const T &first, const T &second), int size) {
};

#endif